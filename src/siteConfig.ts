/**
 * Canonical site origin (no trailing slash).
 * Keep aligned with `public/robots.txt`, `public/sitemap.xml`, and hosting redirects.
 */
export const SITE_CANONICAL_ORIGIN = 'https://amoradigital.nl'

/** Google Analytics 4 measurement ID (gtag in `index.html` must use the same value). */
export const GA_MEASUREMENT_ID = 'G-LF7H7MVWRJ'
